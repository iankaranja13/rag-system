version: '3.8'

services:
  medical-rag:
    build: .
    ports:
      - "8501:8501"
    environment:
      -GEMINI_API_KEY=${GEMINI_API_KEY} 
      - COHERE_API_KEY=${COHERE_API_KEY}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
